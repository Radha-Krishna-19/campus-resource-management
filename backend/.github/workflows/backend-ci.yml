name: Backend CI

on:
  push:
    branches: [ main, master, new-Backend ]
  pull_request:
    branches: [ main, master, new-Backend ]

jobs:
  test:
    runs-on: ubuntu-latest

    services:
      mongodb:
        image: mongo:latest
        ports:
          - 27017:27017

    steps:
    - uses: actions/checkout@v4

    - name: Use Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
        cache-dependency-path: package-lock.json

    - name: Install dependencies
      run: npm ci

    - name: Run tests
      run: npm test
      env:
        MONGO_URI: mongodb://localhost:27017/test_db
        SESSION_SECRET: ci-test-secret
        GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}

  build:
    needs: test
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Use Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
        cache-dependency-path: package-lock.json

    - name: Install dependencies
      run: npm ci

    - name: Build Docker image
      run: docker build -t campus-resource-manager-backend .
